package week10;

public class Problem14 {

	/*
	 * How would you write code to determine whether or not a binary tree is a binary search tree?
	 * 
	 * boolean check(root) {
	 * 		if (root < root.leftChild()) return false;
	 * 		else if (root > root.rightChild()) return false;
	 * 		else return check(root.leftChild()) || check(root.rightChild());
	 * }
	 */
}
